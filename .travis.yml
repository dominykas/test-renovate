language: node_js
sudo: false

cache:
  npm: false

install:
  - "npm install --ignore-scripts"
  - "npx allow-scripts"

#jobs:
#  include:
#    - stage: release
#      if: branch = master AND type = push
#      node_js: "12"
#      deploy:
#        provider: "script"
#        skip_cleanup: true
#        script:
#          - "npx semantic-release"
